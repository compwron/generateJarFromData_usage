apply plugin: 'java'
apply plugin: 'idea'

description = "Example usage of jar containing hierarchies"

repositories {
    mavenCentral()
    maven {
        url project.releaseRepo
    }
}

sourceSets {
    main {
        java {
            srcDirs += 'src'
            srcDirs += 'resources'
        }
    }

    test {
        java {
            srcDirs += 'test'
            srcDirs += 'resources'
        }
    }
}

dependencies {
    compile group: '', name: 'publicHierarchyGenerator', version: 'dev'
    testCompile group: 'junit', name: 'junit', version: '4.+'
    testCompile group: 'org.hamcrest', name: 'hamcrest-all', version: '1.+'
//    compile files('lib/publicHierarchyGenerator-dev.jar')
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.9'
}